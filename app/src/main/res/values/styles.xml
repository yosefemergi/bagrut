<resources>
    <!-- הסגנון הראשי של האפליקציה -->
    <style name="Theme.MyApp" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- צבעים, גופנים והגדרות אחרות -->
    </style>

    <!-- סגנון מותאם לרדיו-באטון -->
    <style name="CustomRadioButton" parent="Widget.MaterialComponents.CompoundButton.RadioButton">
        <item name="android:buttonTint">@color/colorPrimary</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>
    <style name="CustomAlertDialog" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="colorPrimary">@color/green</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:background">@color/grey</item>
        <item name="android:windowBackground">@color/drawerBackground</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
